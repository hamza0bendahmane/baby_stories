<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar.Bridge">">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/color3</item>
        <item name="colorPrimaryDark">@color/backColor</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="splash" parent="Theme.MaterialComponents.Light.NoActionBar.Bridge">">
        <!-- Customize your theme here. -->

        <item name="android:windowBackground">@drawable/splash</item>
    </style>

    <style name="textStyle">

        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">#110011</item>
        <item name="android:textSize">18pt</item>
        <item name="android:textStyle">bold</item>
    </style>

</resources>
